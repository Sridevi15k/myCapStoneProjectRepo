// const { Pool } = require("pg");
// const pool = new Pool({
//   user: "postgres", //username for your postgres db in pgAdmin4 when u set up db.
//   host: "localhost", //localhost because thats where the db is reachable(would be server ip address when hosted)
//   database: "test", //name of the db u wanna connect with
//   password: "Sri@960433", //will be an env when pushed to GH
//   port: 5432 //generic port for pg
// });

// module.exports = pool;
